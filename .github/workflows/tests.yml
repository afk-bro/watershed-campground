name: Test Suite

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - run: npm run lint -- --max-warnings=0

  playwright:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - run: npm ci
      - uses: supabase/setup-cli@v1
        with:
          version: latest
      - name: Start Supabase
        run: npx supabase start
      - name: Load E2E test fixtures
        run: |
          echo "🔄 Loading E2E test fixtures (seed.local.sql)..."
          psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/seed.local.sql || echo "⚠️  seed.local.sql not found, skipping"
          psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres" -f supabase/seed.sql
          echo "✅ E2E fixtures loaded"
      - name: Get Supabase credentials
        run: |
          # Extract credentials from Supabase status using JSON output for reliability
          STATUS_JSON=$(npx supabase status -o json)

          # Extract values using bracket notation for fields with spaces
          API_URL=$(echo "$STATUS_JSON" | jq -r '.["API URL"] // .API_URL')
          ANON_KEY=$(echo "$STATUS_JSON" | jq -r '.["anon key"] // .ANON_KEY // .PUBLISHABLE_KEY')
          SERVICE_KEY=$(echo "$STATUS_JSON" | jq -r '.["service_role key"] // .SERVICE_ROLE_KEY // .SECRET_KEY')

          echo "NEXT_PUBLIC_SUPABASE_URL=$API_URL" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_SUPABASE_ANON_KEY=$ANON_KEY" >> $GITHUB_ENV
          echo "SUPABASE_SERVICE_ROLE_KEY=$SERVICE_KEY" >> $GITHUB_ENV
          echo "TEST_ADMIN_EMAIL=admin@test.com" >> $GITHUB_ENV
          echo "TEST_ADMIN_PASSWORD=testpass123" >> $GITHUB_ENV
          echo "ADMIN_EMAILS=admin@test.com" >> $GITHUB_ENV
          echo "STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY || 'sk_test_dummy' }}" >> $GITHUB_ENV
          echo "NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY || 'pk_test_dummy' }}" >> $GITHUB_ENV
          echo "STRIPE_WEBHOOK_SECRET=${{ secrets.STRIPE_WEBHOOK_SECRET || 'whsec_dummy' }}" >> $GITHUB_ENV

          # Debug: verify extraction succeeded
          echo "Supabase credentials extracted:"
          echo "  API_URL: $([ -n "$API_URL" ] && echo "✓ set" || echo "✗ missing")"
          echo "  ANON_KEY: $([ -n "$ANON_KEY" ] && echo "✓ set" || echo "✗ missing")"
          echo "  SERVICE_KEY: $([ -n "$SERVICE_KEY" ] && echo "✓ set" || echo "✗ missing")"
      - name: Create .env.test for Playwright
        run: |
          cat > .env.test << 'ENVEOF'
          NEXT_PUBLIC_SUPABASE_URL=${{ env.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ env.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY=${{ env.SUPABASE_SERVICE_ROLE_KEY }}
          TEST_ADMIN_EMAIL=${{ env.TEST_ADMIN_EMAIL }}
          TEST_ADMIN_PASSWORD=${{ env.TEST_ADMIN_PASSWORD }}
          STRIPE_SECRET_KEY=${{ env.STRIPE_SECRET_KEY }}
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${{ env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
          STRIPE_WEBHOOK_SECRET=${{ env.STRIPE_WEBHOOK_SECRET }}
          ENVEOF
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium
      - name: Run Playwright Tests
        env:
          # Pass Supabase credentials to Playwright/Next.js webServer
          NEXT_PUBLIC_SUPABASE_URL: ${{ env.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ env.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ env.SUPABASE_SERVICE_ROLE_KEY }}
          TEST_ADMIN_EMAIL: ${{ env.TEST_ADMIN_EMAIL }}
          TEST_ADMIN_PASSWORD: ${{ env.TEST_ADMIN_PASSWORD }}
          ADMIN_EMAILS: ${{ env.ADMIN_EMAILS }}
          STRIPE_SECRET_KEY: ${{ env.STRIPE_SECRET_KEY }}
          NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
          STRIPE_WEBHOOK_SECRET: ${{ env.STRIPE_WEBHOOK_SECRET }}
          # Enable Stripe tests in CI (keys loaded from .env.test)
          STRIPE_TESTS_ENABLED: 'true'
        run: npx playwright test --reporter=list
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
